<Window x:Class="FileLister.View.RenamerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ViewModel="clr-namespace:FileLister.ViewModel"
        xmlns:View="clr-namespace:FileLister.View"
        xmlns:Controls="clr-namespace:Common.Controls;assembly=Common"
        xmlns:Properties="clr-namespace:FileLister.Properties"
        MinWidth="600"
        MinHeight="300"
        Height="300"
        Width="600">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="6*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <TextBox
            Grid.Row="0"
            Grid.ColumnSpan="2"
            Background="Black"
            Foreground="LightGray"
            Text="Renamer" />

        <ListBox
            Grid.Row="2"
            Grid.Column="0"
            ItemsSource="{Binding InputItems}" />
    </Grid>
</Window>